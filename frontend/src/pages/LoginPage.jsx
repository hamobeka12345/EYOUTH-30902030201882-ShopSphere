import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { useAuth } from '../context/AuthContext';
export default function LoginPage(){const {login,register}=useAuth(),nav=useNavigate(),[mode,setMode]=useState('login'),[form,setForm]=useState({name:'',email:'',password:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);const set=(k)=>(e)=>setForm({...form,[k]:e.target.value});const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const u=mode==='login'?await login(form.email,form.password):await register(form.name,form.email,form.password);nav(u.role==='admin'?'/admin':'/products')}catch(e){setError(e.response?.data?.message||'We couldn’t sign you in. Please try again.')}finally{setBusy(false)}};return <main className="page container"><section className="auth"><div className="eyebrow">Welcome to Morrow</div><h1 style={{fontFamily:'Georgia,serif'}}>{mode==='login'?'Welcome back':'Create your account'}</h1>{error&&<p className="notice">{error}</p>}<form className="form-stack" onSubmit={submit}>{mode==='register'&&<input className="field" required placeholder="Name" value={form.name} onChange={set('name')}/>}<input className="field" required type="email" placeholder="Email address" value={form.email} onChange={set('email')}/><input className="field" required type="password" placeholder="Password" value={form.password} onChange={set('password')}/><button className="btn" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button></form><p>{mode==='login'?'New here? ':'Already have an account? '}<button className="text-button" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Create an account':'Sign in'}</button></p></section></main>}
