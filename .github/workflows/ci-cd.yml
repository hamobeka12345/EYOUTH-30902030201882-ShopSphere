name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || github.ref == 'refs/heads/develop' && 'development' || 'staging' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build

      - name: Deploy frontend to Vercel
        uses: vercel/action@v1
        with:
          token: ${{ secrets.VERCEL_TOKEN }}
          org-id: team_fD7XqvKuczK1sDWxGw4q8Qqa
          project-id: prj_0X4ETYGcVEZrQTrww0A3Qvm8dSFu
          working-directory: ./frontend
          production: ${{ github.ref == 'refs/heads/main' }}

      - name: Deploy backend to Vercel
        uses: vercel/action@v1
        with:
          token: ${{ secrets.VERCEL_TOKEN }}
          org-id: team_fD7XqvKuczK1sDWxGw4q8Qqa
          project-id: prj_aTQxTQsHYQSn8EsX9bPEmPUMzeJu
          working-directory: ./backend
          production: ${{ github.ref == 'refs/heads/main' }}

      - name: Deploy review service to Vercel
        uses: vercel/action@v1
        with:
          token: ${{ secrets.VERCEL_TOKEN }}
          org-id: team_fD7XqvKuczK1sDWxGw4q8Qqa
          project-id: prj_S8s9i4dSdeXEEPSm0zcTlzKZpnbS
          working-directory: ./review-service
          production: ${{ github.ref == 'refs/heads/main' }}
